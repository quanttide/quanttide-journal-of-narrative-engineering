# 2026-06-03

## Provider API 完全重写：StyleSample 驱动的 3R

今日完成 Provider API 的第四次重写——从"段落级起承转合分析"演进为"StyleSample 驱动的多维度风格对齐评估"。

### 架构演进史

| 阶段 | 核心模型 | 问题 |
|------|---------|------|
| alpha.1 | 段落级起承转合 + StyleStore | 有状态副作用，LLM stub |
| alpha.2 | 无状态 /review + /reflect + /rewrite + /cycle | 双管线（Flutter 正则 ≠ Provider LLM） |
| alpha.3 | 无状态 /review + 3R 端点 | summary 硬编码，style_examples 硬编码 |
| alpha.4 | Criterion 驱动（positive/negative/constraint） | criteria 模型太扁平，缺乏维度结构 |
| **alpha.5** | **StyleSample 驱动（dimensions + excerpts）** | — |

### 最终 API

| 端点 | 输入 | 输出 |
|------|------|------|
| `POST /review` | `text` + `style{dimensions, excerpts}` | `dimension_alignments[]`（每个维度的对齐分数 + 偏差） |
| `POST /analyze` | `text` + `style` + `dimension_title` | 深度分析：pattern 对比 + root_cause + fix_strategies |
| `POST /inspire` | `text` + `style` + `target_dimensions[]` + `variety` | 多条启发式改写建议 |

### 核心设计决策

**1. Style 是一等公民**

放弃"段落评审"和"通用质量标准"的概念。系统不再内置任何写作规则——用户通过 `Style` 定义"什么算好"。`Style` 包含：

| 字段 | 来源 | 用途 |
|------|------|------|
| `dimensions[]` | `style.yaml` | LLM 的分析框架（叙事视角、语言风格、情感表达等） |
| `excerpts[]` | `style.yaml` | 每个维度的短示例，嵌入 prompt |
| `samples[]` | `sample.md` | 完整样本（预留，暂未接入 prompt） |

**2. 三个端点 ≠ 三个独立工具**

`/review` 做广度（所有维度的概览），`/analyze` 做深度（单个维度的根因），`/inspire` 做创意（基于偏差生成建议）。前端工作流：review → 低分维度 → analyze → 看根因 → inspire → 获取改法。

**3. 集成测试只验证稳定的东西**

14 个集成测试的经验教训：LLM 输出不可预测，唯一可靠的断言是存在性（字段有值）和相对比较（同一请求内 A > B）。试图对 LLM 输出设数值阈值是在和随机性赌博。

### 合并/删除

- 删除了 Flutter 正则引擎作为 `/review` 的 fallback（评审现在统一走 Provider）
- 删除了 `store.py` / `services/review.py` / `services/reflect.py` / `services/rewrite.py`
- 删除了 `/cycle` 端点
- 删除了所有旧模型：`ArticleIn`、`ReviewOut`、`ParagraphReview`、`Comparison` 等
- `examples/default/apps/studio` 合并到 `src/studio`

### 测试现状

| 层级 | 数量 | mock |
|------|------|------|
| 单元测试（`tests/`） | 6 | `call_llm` 全 mock |
| 集成测试（`integrated_tests/`） | 14 | 不 mock，真实 DeepSeek API |
| Flutter（`src/studio`） | 125 | 各自 mock |

### Harness 认知修正

今日在集成测试上花了 5 轮迭代，核心教训不是在测试怎么写，而是对 Harness 的理解偏差：

**我之前以为的 Harness** = 管理 LLM 调用的基础设施层（重试、日志、超时、token 控制）

**实际 Harness** = 测试设计者头脑里的约束集

具体来说，LLM 测试的 Harness 不是框架提供的，是写测试的人必须遵守的模式：

| 模式 | 约束 | 解决什么问题 |
|------|------|-----------|
| 同轮相对比较 | 不 assert 绝对阈值，只 assert 同一请求内的排序 | LLM 输出数值不稳定 |
| 交叉验证 | 正反两个方向都测 | 单次偶然性偏差 |
| 负样本控制 | 中性文本必须低分 | 基线漂移检测 |
| 存在性断言 | 只检查字段有值，不检查值的范围 | 输出格式不稳定 |
| 失败可追溯 | 每次 LLM 调用记录 prompt + response | 无法区分 LLM 挂 vs 逻辑错 |

这些约束不能通过装一个库获得。它能且只能通过写测试的人内化来解决——这正是今天 5 轮迭代中从"设阈值"到"删阈值"到"只比相对值"的认知路径。

### 未完成

- [ ] `style.samples` 字段暂未接入 prompt
- [ ] `integrated_tests/conftest.py` 的 API Key 跳过逻辑仅检查 env var，不区分"没 Key"和"Key 错误"
- [ ] Inspire 的 `conservative vs creative` 验证通过，但"不匹配风格建议应更实质"的前提被推翻（实际匹配风格建议反而更长）
